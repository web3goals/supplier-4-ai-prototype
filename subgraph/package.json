{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kiv1n/dataset-supplier",
    "create-local": "graph create --node http://localhost:8020/ kiv1n/dataset-supplier",
    "remove-local": "graph remove --node http://localhost:8020/ kiv1n/dataset-supplier",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kiv1n/dataset-supplier"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.2",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
